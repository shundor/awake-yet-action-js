name: Example Action

on:
  issues:
    types: [opened]
  pull_request:
    branches: [main]
    types: [opened]

jobs:
  comment:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Comment
        id: awake-yet-action-js
        uses: shundor/awake-yet-action-js@master
